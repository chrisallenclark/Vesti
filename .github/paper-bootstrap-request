# Editing this file runs the one-time paper-trading bootstrap.
#
# It records opening capital, and that must happen exactly once: a second
# deposit asserts money that was never transferred, and every position size
# after it is sized against a portfolio that does not exist. Do not edit this
# to "re-run" anything — the daily session is `.github/paper-request`.

bootstrap: 2 — retry; the first attempt failed before recording any capital
