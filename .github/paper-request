# Editing this file runs one session immediately, outside the schedule, and
# republishes the site. Safe to repeat: the session is idempotent, so a re-run
# finishes the day's work rather than doubling it.

run: 2 — verify the session runs now that publishing is a separate workflow
