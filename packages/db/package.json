{
  "name": "@vesti/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "migrate": "tsx --env-file-if-exists=../../.env src/migrate.ts up",
    "reset": "tsx --env-file-if-exists=../../.env src/migrate.ts reset",
    "status": "tsx --env-file-if-exists=../../.env src/migrate.ts status",
    "test": "tsx --env-file-if-exists=../../.env --test src/*.test.ts"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  }
}
